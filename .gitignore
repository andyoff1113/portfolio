.Rproj.user/
.Rhistory
.RData
.Ruserdata

# Not for public repo (copyrighted PDFs / personal documents)
References/
研究計画書_v3.docx

# Regenerable knit intermediates in src/ (docs/*_files/ is the deployed
# output and must stay tracked, so this only targets the source side)
_site/
*_cache/
src/*_files/
